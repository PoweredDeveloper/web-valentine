# Run from project root so .env (same folder as this file) is found.
# Create .env with TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID if missing.
services:
  app:
    build: .
    container_name: valentine-app
    env_file:
      - .env
    networks:
      - proxy-net
    restart: unless-stopped

networks:
  proxy-net:
    external: true
